{
  "data": [
    [
      "MYSQL",
      "'SELECT @@version",
      "Displays version of the database"
    ],
    [
      "MYSQL",
      "'SELECT user();",
      "Shows the current user running this query, note this is different from system user."
    ],
    [
      "MYSQL",
      "'SELECT system_user();",
      "Shows the system user, note this is different from current user running the queries."
    ],
    [
      "Oracle",
      "'SELECT banner FROM v$version WHERE banner LIKE ‘Oracle%’;",
      "Displays the current version of the database."
    ],
    [
      "MYSQL",
      "'SELECT database()",
      "Displays the current database."
    ],
    [
      "MYSQL",
      "'SELECT user FROM mysql.user; — priv",
      "Lists all database users"
    ],
    [
      "MYSQL",
      "'SELECT if(1=1,’foo’,'bar’);",
      "If Statement - in this case if 1=1 then return foobar"
    ]
    
    [
      "MYSQL",
      "'SELECT schema_name FROM information_schema.schemata;",
      "List Databases for MySQL > v5.0"
    ]
    
    [
      "MYSQL",
      "'SELECT distinct(db) FROM mysql.db",
      "List Databases for MySQL > v5.0"
    ]
  ]
}
